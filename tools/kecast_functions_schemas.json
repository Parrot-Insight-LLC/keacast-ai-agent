
[
    {
      "name": "getUserAccounts",
      "description": "Fetch all bank accounts for a user.",
      "parameters": {
        "type": "object",
        "properties": {
          "userId": { "type": "string", "description": "ID of the user" }
        },
        "required": ["userId"]
      }
    },
    {
      "name": "getAccountDetails",
      "description": "Get detailed information about a specific account.",
      "parameters": {
        "type": "object",
        "properties": {
          "userId": { "type": "string" },
          "accountId": { "type": "string" }
        },
        "required": ["userId", "accountId"]
      }
    },
    {
      "name": "getUserTransactions",
      "description": "Retrieve all transactions for a user's account.",
      "parameters": {
        "type": "object",
        "properties": {
          "userId": { "type": "string" },
          "accountId": { "type": "string" }
        },
        "required": ["userId", "accountId"]
      }
    },
    {
      "name": "getRecurringForecasts",
      "description": "Retrieve all recurring forecast transactions for a specific account.",
      "parameters": {
        "type": "object",
        "properties": {
          "accountId": { "type": "string" }
        },
        "required": ["accountId"]
      }
    },
    {
      "name": "getUpcomingTransactions",
      "description": "Retrieve upcoming transactions for a given account and time range.",
      "parameters": {
        "type": "object",
        "properties": {
          "accountId": { "type": "string" },
          "startDate": { "type": "string" },
          "endDate": { "type": "string" },
          "forecastType": { "type": "string" },
          "currentDate": { "type": "string" }
        },
        "required": ["accountId", "startDate", "endDate", "forecastType", "currentDate"]
      }
    },
    {
      "name": "getUserCategories",
      "description": "Retrieve all categories created by a user.",
      "parameters": {
        "type": "object",
        "properties": {
          "userId": { "type": "string" }
        },
        "required": ["userId"]
      }
    },
    {
      "name": "getUserSubscription",
      "description": "Get a user's subscription plan details.",
      "parameters": {
        "type": "object",
        "properties": {
          "subscriptionId": { "type": "string" }
        },
        "required": ["subscriptionId"]
      }
    },
    {
      "name": "getPlaidTransactions",
      "description": "Get transactions from Plaid API using access token.",
      "parameters": {
        "type": "object",
        "properties": {
          "accessToken": { "type": "string" }
        },
        "required": ["accessToken"]
      }
    },
    {
      "name": "getPlaidBalances",
      "description": "Get real-time balances from Plaid API using access token.",
      "parameters": {
        "type": "object",
        "properties": {
          "accessToken": { "type": "string" }
        },
        "required": ["accessToken"]
      }
    },
    {
      "name": "getRecurringBankTransactions",
      "description": "Get recurring bank transactions from Plaid.",
      "parameters": {
        "type": "object",
        "properties": {
          "accessToken": { "type": "string" },
          "accountId": { "type": "string" }
        },
        "required": ["accessToken", "accountId"]
      }
    },
    {
      "name": "getShoppingList",
      "description": "Retrieve the shopping list for a given user.",
      "parameters": {
        "type": "object",
        "properties": {
          "userId": { "type": "string" }
        },
        "required": ["userId"]
      }
    }
  ]
  